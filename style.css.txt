/* style.css */

/* Optional custom hover/animation */
.cell:hover {
  transform: scale(1.1);
  transition: transform 0.2s ease-in-out;
  cursor: pointer;
}

/* Add text shadow for readability */
.text-shadow-lg {
  text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.6);
}